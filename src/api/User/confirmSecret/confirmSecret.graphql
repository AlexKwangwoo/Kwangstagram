type Mutation {
  confirmSecret(secret: String!, email: String!): String!
}
type Mutation {
  confirmSecret(secret: String!, email: String!): String!
}
type Mutation {
  confirmSecret(secret: String!, email: String!): String!
}
